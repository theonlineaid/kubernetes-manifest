apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: app
data:
  NODE_ENV: "production"
  APP_PORT: "3000"
  API_URL: "http://backend-app:5000"   # k8s Service name for the backend, DNS-resolvable in-cluster
